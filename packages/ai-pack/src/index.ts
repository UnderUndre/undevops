/**
 * @undevops/ai-pack — AI review provider toolkit
 *
 * Provides:
 * - AIReviewerProvider interface and implementations (Claude, Gemini, OpenAI, Codex, Custom)
 * - Change payload builder (diff extraction, env var change detection)
 * - Gate evaluator (parallel review, strict-by-default policy)
 * - Verdict types and scoring
 */

export const AI_PACK_VERSION = "0.1.0";
