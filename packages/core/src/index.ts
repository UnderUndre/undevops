/**
 * @undevops/core — Core deployment engine
 *
 * Extracted from @undevops/server. Contains:
 * - Deployment orchestration (build pipeline, Docker integration, health checks)
 * - Server management (SSH connectivity, health monitoring, registration)
 * - Traefik/proxy integration (reverse proxy config, TLS, routing)
 * - Secret encryption (AES-256-GCM)
 * - Audit event recording (structured events, actor attribution)
 * - Auth core (better-auth integration, session management)
 */

// Placeholder — extraction from @undevops/server happens in Phase 2 (T027–T032)
export const CORE_VERSION = "0.1.0";
