# SpecKit Review: 001-init (undevops) — Full Artifact Set

**Reviewer**: claude
**Reviewed at**: 2026-05-25T16:15:00Z
**Commit**: 6bf09a25d (HEAD; plan.md, tasks.md, data-model.md, research.md, quickstart.md, contracts/ are uncommitted in working tree)
**Artifacts reviewed**: spec.md, plan.md, tasks.md, data-model.md, research.md, quickstart.md, contracts/mcp-api.md, contracts/plugin-sdk.md, reviews/analyze.md, reviews/antigravity.md, .specify/memory/constitution.md

## Summary

This review **supersedes** my earlier review (committed `1bc0f35`, tagged `review/001-init/v1`) which was incorrectly performed on `spec.md` alone — `plan.md` and `tasks.md` were already present in the working tree but untracked, and I missed them. Apologies. The earlier review's 2 procedural CRITICAL findings are withdrawn; the substantive findings about the spec remain valid where the plan didn't address them.

The plan + tasks + data-model are **substantially more complete than the spec alone suggested** — 136 tasks, dependency graph, agent routing, parallel lanes, full Drizzle schema with migrations, MCP contracts. Quality of work is high. But the new artifacts introduce new gaps and **contradictions between layers** that neither `analyze.md` nor `antigravity.md` flagged.

Other reviewers in this wave:
- **`reviews/analyze.md`** (Claude self-consistency, verdict **HIGH**): focused on requirement-to-task coverage. Found pluggable auth interface undefined, plan.md doc-tree mismatch, missing startup reconciliation, missing Windows verification, missing HTTP version endpoint. Strong coverage-mapping work.
- **`reviews/antigravity.md`** (verdict **CRITICAL**): focused on security/architecture. Found pattern-based secret redaction is broken, in-process plugin sandboxing is cosmetic, SSE streams survive token revocation, zero-downtime deadlock, multi-server HA quorum. Sharp adversarial work.

This review **avoids re-litigating** what both already covered and concentrates on **new findings across the now-complete artifact set**, with brief reinforcement where I have new specificity to add.

**Independence caveat** (still applies): this review is written by the same Claude Code session that authored `spec.md`. Less of a blind-spot risk now because plan/tasks/data-model were authored by a different session. But `/speckit.implement` gate per constitution Principle VI still benefits from at least one more distinct external reviewer (Codex Desktop, Gemini CLI, or Copilot) — `antigravity.md` alone is the only truly independent voice so far. With the gate needing **two** distinct external PASS verdicts and Antigravity returning CRITICAL, **the gate is currently failing**.

## NEW Findings (not in analyze.md or antigravity.md)

| ID | Severity | Area | Finding | Recommendation |
|----|----------|------|---------|----------------|
| F1 | **CRITICAL** | Cross-artifact contradiction at security boundary | The MCP token scope enum is **defined three incompatible ways across artifacts**: `spec.md` clarification Q3 says `(access-level: read / write / exec) × (target: per-project | all-projects)`; `data-model.md` defines `mcpAccessLevel = ['read', 'deploy', 'admin']` and stores scope as a single column (NOT cross-product with target); `quickstart.md` step 1 shows `undevops mcp-tokens create --scope "read,write"` implying a **list** of scopes. Three sources, three different mental models. Whichever ships first becomes truth and the others silently rot. This is the headline security boundary of the platform | Pick one model and reconcile all three. Recommendation: spec's model is the most expressive — `(access-level: read / write / exec) × (target: per-project | all-projects)`. Update `data-model.md` enum to match spec; update `quickstart.md` example to show single scope per token; if a token needs multiple scopes, the user creates multiple tokens. Add a contract test asserting all three artifacts use the same enum names |
| F2 | **CRITICAL** | Data integrity / migration | `data-model.md` Migration 004 includes a SQL data-migration block that copies rows from the existing `ai` table into `ai_reviewers` with `credentialRef = 'migrated:ai:' || aiId`. The text under the migration says *"After verification, create a `secret` row for each migrated AI API key and update `credentialRef` accordingly."* — **but no task in `tasks.md` does this**. Result: after Migration 004 runs, every migrated `ai_reviewer` row references a non-existent secret. First call to `T099 ClaudeReviewer` or any sibling will fail to resolve credentials at runtime. Reviewers will silently return ABSENT → under FR-025a strict default, all gated deploys block. The failure mode is "AI gate goes from working to never working" with no obvious code-level cause | Add a task between T017 (create `ai_reviewers` table) and T106 (gate evaluator): "Implement and execute a one-shot migration that creates a `secret` row for each pre-existing `ai` row's API key, then UPDATEs the corresponding `ai_reviewer.credentialRef` to point at it." Or, simpler: skip the auto-migration entirely and require admins to re-create AI reviewers post-upgrade via UI |
| F3 | HIGH | Security drift over time | `research.md` RQ-010 + `plan.md` Dependencies section state: *"Dokploy upstream | v0.29.5 | Fork point; no further upstream merges planned for v0.x"*. This means undevops will **not receive upstream security patches** from Dokploy. The largest single category of vulnerabilities you inherit on day 1 will not be patched as Dokploy fixes them. For a deployment platform with TLS handling, SSH, container orchestration, and auth — this is a slow-burning critical risk that compounds with every Dokploy CVE | At minimum: add an Assumption + FR mandating a monthly review of Dokploy's CHANGELOG / security advisories with explicit decision-log entry. Better: define a "selective upstream cherry-pick policy" — security fixes only, not features — and a task in Phase 9 to implement it as a documented procedure. Don't ship "frozen fork" as the permanent policy |
| F4 | HIGH | Scope drift between spec and contracts | `spec.md` FR-020 limits MCP write actions to **deploy / rollback / scale**. `quickstart.md` advertises MCP operations including `undevops_inspect_deployment`, `undevops_get_logs`, `undevops_diagnose_deployment`, `undevops_list_projects` — fine, those are read-scope. But `data-model.md` `agentActionType` enum includes `'redeploy', 'stop', 'start', 'restart', 'env_update'` in addition to deploy/scale — **5 actions beyond what FR-020 permits**. `tasks.md` T086-T089 implement only the FR-020 subset. Either FR-020 is too narrow (write spec doesn't allow what data model permits) or the enum is over-built (lying about what undevops will do). Pick one | If the broader action set is intended: update FR-020 to explicitly enumerate all 7 action types and add tasks for the missing 5. If the narrow set is intended: shrink `agentActionType` to `['deploy', 'rollback', 'scale']` to prevent future scope creep. **Don't ship with an enum that promises capabilities the FR doesn't authorize** — this is how unauthorized features ship by accident |
| F5 | HIGH | Audit-log tamper-evidence still absent | My original spec review (F6) flagged: no FR/SC requires audit log to be append-only or tamper-evident. The plan + data-model **still don't address this**. `audit_log` is a normal Postgres table — any role with UPDATE/DELETE on it can rewrite history. For a platform whose multi-AI review feature is positioned as compliance-grade, this is the single largest credibility gap. Forging an "agent X did Y" event is a one-line UPDATE | Add an FR + tasks: (a) database-level `REVOKE UPDATE, DELETE ON audit_log FROM <role>` via migration; (b) optionally a hash-chain (each row stores SHA-256 of previous row + own data), with a periodic integrity-scan job. The constitutional precedent for this exists — Audit Log Immutability via SQL RLS Grant/Revoke was already done elsewhere in this org per the session-start context |
| F6 | HIGH | MCP transport security still implicit | `spec.md` FR-013 specifies bearer tokens but no FR or task mandates TLS-only transport. `plan.md` mentions Traefik for **deployed apps** but doesn't explicitly say MCP server binds behind TLS. T057/T058 implement stdio (LAN/local) and SSE (HTTP) transports. **SSE over cleartext HTTP would leak bearer tokens to any on-path observer** — a CVE-class default | Add to T058: SSE transport MUST refuse cleartext bind; require TLS termination at Traefik for the MCP endpoint. Add an SC test asserting `curl http://...` against the MCP endpoint returns a redirect or refusal. Document the stdio transport's threat model separately (it's exec-into-controller, different attack surface) |
| F7 | HIGH | `packages/core` boundary enforcement is one-time, not continuous | `tasks.md` T035: *"Add CI gate: packages/core builds without packages/ai-pack present"*. **Mechanism is undefined.** Read literally, this is a CI step that deletes `packages/ai-pack/` and runs `pnpm build --filter @undevops/core`. That's a one-time build check. It does NOT prevent a developer from adding `import { foo } from "@undevops/ai-pack"` inside `packages/core/src/something.ts` because in dev mode ai-pack IS present. The forbidden import sits silently until next CI run. A continuous enforcement is needed (linter rule, dependency-cruiser config, or workspace-level forbid) | Replace or augment T035 with a continuous boundary enforcement: e.g. `eslint-plugin-boundaries` rule that errors on cross-package imports from core to ai-pack, OR a `dependency-cruiser` config file. The CI gate stays as defense-in-depth but isn't the primary mechanism. Otherwise the open-core-readiness ratchet (SC-005) is illusion |
| F8 | HIGH | Restore stops applications — spec says they continue | `spec.md` edge case says: *"The control plane being temporarily unavailable does NOT take down already-running deployed applications — they continue serving traffic via the reverse proxy independently of the control plane's liveness."* `quickstart.md` step 4 of Backup section says: *"Restore process: ... Applications are stopped during restore."* These contradict. Restore is a window where control plane is being rebuilt — under spec, deployed apps should keep serving; under quickstart, they don't. T125 (restore command) doesn't specify behavior either way | Reconcile: either (a) update spec edge case to acknowledge that **restore** is a special case where apps may be stopped (which means SC-011 RTO needs to include app-downtime caveat); or (b) implement restore so apps continue running (do everything in-place via fresh DB → migration → app reconnect, never stop running containers). Quickstart and spec must say the same thing |
| F9 | HIGH | Migration 005 / pending-action cleanup worker missing | `data-model.md` Migration 006 defines `pending_agent_actions` with `expires_at` and a partial index `pendingAction_expiresAt_idx ... WHERE status = 'pending'` plus `pendingActionStatus` enum value `'expired'`. **No task implements the worker that transitions stale rows from `pending` → `expired`.** Without the worker: pending-actions table grows unbounded, the partial index becomes irrelevant (no rows transition out), the `expires_at` field is decorative. Agents see actions "pending" indefinitely | Add to Phase 9 (Polish): "BullMQ scheduled job in `apps/scheduler/`: every 5 minutes, scan `pending_agent_actions` for `status='pending' AND expires_at < NOW()`, update to `status='expired'`, audit-log the auto-expiry, send SSE notification to creator if any subscriptions exist" |
| F10 | HIGH | Audit log retention vs scale envelope | `spec.md` SC-012 commits to 30-day audit/log retention at scale envelope (50 servers, 500 projects). At that scale, audit volume is dozens of events per project per day → low estimate 50 × 500 × 10 = 250K events/day = 7.5M events/month. **No task implements retention rollover / archival / pruning.** The `audit_log` table grows unbounded. By month 3 the table has 22M rows and queries through it slow non-linearly, violating SC-002 (MCP read p95 < 500ms via audit-log resource T063). The 30-day commitment in SC-012 is unbacked | Add Phase 9 task: implement audit-log archival job — daily, move rows older than 30 days to compressed S3 or a separate archive table; original table stays small. Also: add a SC test that exercises the retention bound (populate to 30 days × envelope volume, verify SC-002 holds, verify older data is in archive) |
| F11 | HIGH | Plugin permission TAXONOMY still open (carry-over from spec review F12) | `data-model.md` stores `plugins.grantedPermissions text[]` and `manifestJson.permissions: string[]`. **No closed enumeration.** Plugin can declare `'destroy:everything'` and it'd be stored. Permission strings are advisory, not enforced. `quickstart.md` example shows `"permissions": ["deploy:read", "project:read"]` — implying a structure (`<resource>:<verb>`) but no contract defines the valid set | Enumerate the v0.x plugin permission set as an enum in `data-model.md` and `contracts/plugin-sdk.md`. Suggested starting set: `['read:projects', 'read:deployments', 'read:logs', 'read:metrics', 'read:servers', 'notify:webhook', 'notify:slack']`. Reject unknown permission names at install time (T073). Add SC: a plugin with unknown permission `'fake:perm'` fails to install |
| F12 | MEDIUM | Backup uses pg_dump without consistency guard | T124: *"Implement backup job: pg_dump → AES-256-GCM encrypt → S3 upload"*. `pg_dump` defaults to a non-transactional dump — under load, it can capture inconsistent state (table A snapshotted before deploy X, table B after). For a control plane mid-deploy, FK constraints in the restored DB can be violated | Use `pg_dump --single-transaction` (acquires a single transaction snapshot for the whole dump). Or use logical replication snapshots. Document the consistency model in the restore command (T125) |
| F13 | MEDIUM | Restore schema-version mismatch undefined | My original F20 wasn't addressed in plan/tasks. T125 (restore command) doesn't define behavior when the backup's schema version ≠ the current undevops's schema version. Common scenario: restoring an older backup onto a newer undevops install (after admin upgraded undevops between backup and restore) | Specify: restore MUST be allowed when target undevops version ≥ backup version, with automatic schema migration on first start after restore. Restore MUST be rejected when target < backup version (downgrade). Add an SC test |
| F14 | MEDIUM | AI reviewer cost / rate-limit unacknowledged | My original F21 carry-over: multi-AI review sends payloads to N providers per gated deploy. At scale envelope (500 projects, busy teams), this can hit provider rate limits and accumulate substantial token cost. Plan adds `timeoutSeconds` per reviewer (good) but no cost tracking, no rate-limit awareness, no admin observability into spend | Add an FR or Assumption: cost of AI reviewers is admin's responsibility; document the per-deploy token cost order-of-magnitude in `quickstart.md`. Add a per-environment rate-limit on review submissions (e.g. max 10 reviews/minute per provider). Surface cumulative invoke counts in `ai_reviewers.invokeCount` (already in schema) via UI |
| F15 | MEDIUM | Drizzle migration `ai` → `ai_reviewer` is destructive without rollback | Migration 004 says *"After verification, create a `secret` row for each migrated AI API key and update `credentialRef` accordingly. ... `ai` table deprecated"* — but doesn't explicitly say when the `ai` table is dropped or how an admin rolls back if Migration 004 corrupts. The deprecation timing matters | Define the deprecation window: keep `ai` table read-only for one full undevops MINOR cycle, then drop in Migration 00X. Provide a `pg_dump`-style backup of pre-migration state. Document rollback procedure in `tasks.md` T025 |
| F16 | MEDIUM | T136 quickstart validation references nonexistent release infra | T136: *"Run quickstart.md validation: follow guide end-to-end on fresh VPS, verify < 15 min (SC-001)"*. `quickstart.md` step 1 is `curl -fsSL https://get.undevops.com | bash`. There is **no task that builds the installer, publishes the script, or registers the `get.undevops.com` domain**. T136 is impossible to execute as-written until release infrastructure exists | Either: (a) add Phase 9 tasks for release infra (build installer, publish to a CDN, register domain, set up Docker Hub image push); or (b) reword T136 to validate via "manual install from cloned repo" path which `quickstart.md` already documents as the alternative. Don't ship a quickstart that references infra that doesn't exist |
| F17 | MEDIUM | Concurrent test parallelism — file ownership not declared | `tasks.md` declares 22 parallel lanes (§Parallel Lanes). Many lanes touch overlapping files: lane 7 (US1 FE) writes `apps/web/src/components/` and lane 11 (US2 FE) also writes there; lane 6 (US1 BE) writes `packages/core/src/deploy/` and lane 18 (US6 BE) also writes there (`T119` adds load-balancing). Without explicit file ownership per task, parallel agents will race and produce merge conflicts when their work is integrated | Add to `tasks.md` either: (a) per-task "Files Touched" listing (e.g. `T039: packages/core/src/deploy/orchestrator.ts, deploy/build.ts`); or (b) a lane-level "Files Owned" rule that prevents two lanes from touching the same file without explicit handoff. Without this, the dependency graph guarantees order but not isolation |
| F18 | MEDIUM | `verdictType` enum mixes "abstain" (chose not to opine) with "error" (timeout) — but spec says ABSENT | `data-model.md` defines `verdictType = ['pass', 'fail', 'abstain', 'error']`. The enum-doc says: *"abstain — Reviewer has no opinion (treated as pass for 'single' policy, fail for 'unanimous'); error — Reviewer failed to respond (timeout, API error) — treated as fail per default policy"*. But `spec.md` Q4 clarification specifies a single state: **ABSENT** = "did not respond within timeout, counts as FAIL". Mixing "chose not to opine" (semantic) with "couldn't respond" (operational) under one enum invites bugs where a slow reviewer is treated as "thoughtful abstention" | Either: rename `verdictType` enum values to match spec (`pass / fail / absent`) and drop the abstain/error distinction; OR keep the distinction and explicitly map both to ABSENT in spec terminology, documenting why undevops needs to distinguish them internally (e.g. "abstain" rolls into "absent" for gate eval but is logged separately for retrospective) |
| F19 | MEDIUM | `mcp_clients.requestCount` row-update hot spot | Per-token `requestCount` is incremented on every authenticated MCP request via the auth middleware (T037, T066 references). At even modest throughput (10 req/sec per token), this is a postgres row-update hot spot — each increment takes a row lock, serializing requests for that token. Bandwidth ceiling will be PG, not network | Mitigate via one of: (a) Redis-backed in-memory counter with periodic flush to PG (every 60s); (b) PG `UPDATE ... WHERE id = X SET requestCount = requestCount + 1` with `FOR NO KEY UPDATE` — still serializes but avoids unrelated locks. Best: don't include `requestCount` in the auth-middleware critical path; surface "approximate" counter via a separate read model |
| F20 | MEDIUM | `aiProviderType` enum conflates model brand with vendor | `data-model.md` enum: `['claude', 'openai', 'gemini', 'codex', 'custom']`. Issues: "claude" is Anthropic's model family (vendor = Anthropic); "codex" isn't a separate vendor (Codex Desktop wraps OpenAI). The enum mixes the dimensions and will need migration when a fifth model brand emerges or when a "claude via Bedrock" path is added | Restructure: `provider` (vendor enum: `anthropic / openai / google / azure / bedrock / vertex / custom`) + `modelFamily` (string, free-form: `claude-sonnet-4-7 / gpt-5 / gemini-2-flash / ...`). This is more rows now but doesn't break when GPT-6 ships or when an org wants Claude via AWS Bedrock. Inheritable as an extension to existing schema without breaking the v0.x enum |
| F21 | MEDIUM | No formal threat model | For a self-hosted DevOps platform with credential handling, container orchestration, and AI-controllable infrastructure, the absence of any STRIDE/attack-tree/threat-model document in `research.md` or `specs/` is conspicuous. Antigravity caught two architectural threats (F1 secret redaction, F2 plugin sandboxing) that a threat-model exercise would have surfaced upfront | Add a `threat-model.md` to `specs/001-init/` (or `specs/main/`) covering at minimum: actors (admin, agent, plugin author, attacker-with-network-access, attacker-with-DB-access, malicious-AI-reviewer, malicious-plugin); assets (secrets, audit log, deployment state, server credentials); STRIDE per asset. This is one-page work, blocks no tasks, and would have caught F1/F2 before plan stage |
| F22 | MEDIUM | better-auth abstraction layer may fight the framework | T032: *"Extract auth core (better-auth) from packages/server → packages/core/src/auth/ ... Define pluggable auth interface: IAuthProvider with authenticate/authorize/invalidate ... Document extension points for future SSO/RBAC adapters"*. better-auth has its own plugin architecture and extension points. Adding another abstraction layer (`IAuthProvider`) on top may make future SSO/RBAC additions harder, not easier — implementer fights both abstractions | Reconsider: either (a) drop the `IAuthProvider` layer and document better-auth's extension points directly as the FR-007 boundary; or (b) commit to the abstraction but explicitly map each `IAuthProvider` method to a better-auth concept with concrete examples. The current vague "pluggable interface" is the worst of both worlds |
| F23 | LOW | "scope" term is overloaded across tables | In `mcp_clients`, "scope" means **access level** (`mcpAccessLevel`). In `secrets`, "scope" means **resource type** (`secretScopeType`). Same word, two unrelated concepts at the database level. Readers will get confused; queries that try to "filter by scope" will be wrong | Rename one. Suggest: `mcp_clients.scope` → `mcp_clients.accessLevel`; keep `secrets.scope`. Quickstart and CLI prose follow |
| F24 | LOW | Plugin manifest schema duplicated in data-model and contracts | `data-model.md` declares `PluginManifest` TypeScript interface. `contracts/plugin-sdk.md` (per references) also defines the manifest schema. If both are hand-maintained, they drift | Single source of truth: `contracts/plugin-sdk.md` declares the canonical Zod schema; `data-model.md` references it as `manifestJson jsonb $type<PluginManifest>` where `PluginManifest` is imported, not redefined |
| F25 | LOW | Russian inline phrase in spec (carry-over F24) | US2 acceptance: *"почему мой prod пятисотит"*. Fun, in-persona, but cross-AI reviewers in English contexts (Codex, Gemini CLI) lose readability. Antigravity reviewer handled it fine but it's still a friction point for OSS contributors | Translate inline: *"why is my prod returning 500s (in Valera's idiom: «почему мой prod пятисотит»)"*. Persona preserved, accessibility added |
| F26 | LOW | No Glossary section (carry-over F25) | Terms used without definition: AI-native, MCP, open-core, replica, verdict, ABSENT, gate, fault, ai-pack, plugin-sdk, plugn. Cross-AI readers will infer, sometimes wrongly | Add 12-line Glossary subsection between Assumptions and Out-of-Scope |

## REINFORCED Findings (from other reviewers, with new specificity)

| ID | Severity | Original | New angle to add |
|----|----------|----------|------------------|
| R1 | **CRITICAL** | Antigravity F1 — pattern-based secret redaction is broken | Confirmed and extended: `tasks.md` T038 explicitly says *"traverse response JSON, match field patterns per contracts/mcp-api.md §Secret Redaction, replace values with `***REDACTED***`"*. This is pattern-based exactly as Antigravity flagged. **Additional vector**: same flaw applies to the AI-reviewer change-payload boundary (FR-024). Compose files with inline secret values (`environment: { DB_PASS: "..." }`) leak to external AI providers under the same broken mechanism. Fix value-based redaction once, apply at BOTH boundaries (MCP responses + AI reviewer payloads) |
| R2 | HIGH | Antigravity F2 — in-process plugins have controller-level trust | Confirmed. `tasks.md` T073 dynamic-imports plugin modules. T075's permission system is cosmetic against `process.env.UNDEVOPS_ENCRYPTION_KEY` access. **Additional fix path** (since Antigravity offered webhooks/separate-container alternatives): use Node.js Worker threads with explicitly enumerated permission grants — heavier than current design, lighter than container isolation, preserves the TS-native plugin DX. Either way, document the chosen trust boundary in `contracts/plugin-sdk.md` explicitly |
| R3 | HIGH | Antigravity F3 — SSE survives token revocation | Confirmed. T058 (SSE transport) + T037 (auth middleware) have no kill-on-revoke handshake. **Additional**: this is also relevant for the audit-log integrity (R3 covered SSE; same applies if MCP exposes WebSocket-style any-other-streaming primitive later). Suggest a generic "TokenRevocationBus" — Redis pub-sub channel that all transport adapters subscribe to. T067 (revoke command) publishes; T057/T058 (transports) listen and tear down |
| R4 | HIGH | analyze.md U1 — pluggable auth interface undefined | Reinforce: T032 mentions "pluggable boundary for future SSO/RBAC" without defining the boundary. See my F22 above — recommend dropping the abstraction OR committing to it concretely. analyze.md left this open; my F22 proposes the concrete fork |
| R5 | MEDIUM | analyze.md I3 — missing startup reconciliation | Reinforced; spec edge case explicitly requires it. Suggest the task description: "On controller start: enumerate running containers via Docker API, compare against `deployment` table for in-flight/healthy state, surface discrepancies in UI + audit log + MCP `undevops://reconciliation-report` resource. Do NOT auto-redeploy unless `RECONCILE_AUTO_REDEPLOY=true`." |

## Carry-over Findings from My Earlier (spec-only) Review

These remain valid after reading plan + tasks:

- **F9 (HIGH→MEDIUM)** Backup encryption key escrow: `quickstart.md` now documents "permanently unrecoverable" warning prominently. Downgraded MEDIUM because the warning is appropriate and visible — but key-escrow / multi-key support is still absent, which a future enterprise tier would need
- **F11 (MEDIUM)** `.claude/` upstream sync discipline: still not addressed. undevops will silently drift from `underhelpers` upstream as the latter evolves
- **F17 (LOW)** Wildcard certificates (DNS-01 challenge): inherited from Dokploy, plan doesn't explicitly include or exclude. Worth a `quickstart.md` note about what's supported
- **F22 (MEDIUM)** Deploy queue collapse for hotfix workflow: T045 doesn't address. The "collapse to latest commit" rule wrongly elides intentional version rollback via deploy-of-prior-commit

## Constitution Alignment (re-checked)

| Principle | My Assessment | Notes |
|-----------|---------------|-------|
| I — Source of Truth | **PASS** | No `.claude/` source edits in this feature |
| II — Transformer, Not Fork | **PASS** (with caveat) | Plan's "N/A" framing is correct (undevops is a consumer of `.claude/`, not adding a new AI tool target). But the consumer is responsible for sync discipline (carry-over F11) which is currently unspecified |
| III — Protected Slots | **PASS** | |
| IV — SemVer | **PASS** (informal) | Plan documents v0.29.5 → forward as pre-1.0. Could be more explicit per my carry-over F10 |
| V — Token Economy | **PASS** | 5 apps + 4 packages all justify their existence via FR mapping |
| VI — Cross-AI Review | **CURRENTLY FAILING** | Gate requires `analyze.md` PASS + ≥2 external PASS. Current state: `analyze.md` = HIGH (not PASS), `antigravity.md` = CRITICAL (not PASS), `claude.md` = CRITICAL (this review). **Gate is failing on multiple counts**. Must address findings, re-review, before `/speckit.implement` can proceed (or use `--override-gate <reason>` and accept the logged risk) |
| VII — Artifact Versioning | **PARTIAL** | `spec/001-init/v1`, `clarify/001-init/v1`, `review/001-init/v1, v2` exist. But `plan/001-init/*`, `tasks/001-init/*` tags don't yet exist because plan.md and tasks.md aren't committed. The `/speckit.plan` and `/speckit.tasks` workflows include snapshot steps — they didn't run, suggesting those commands didn't complete cleanly |
| VIII — Self-Maintaining | **N/A** | No knowledge artifacts at plan stage |

## Coverage Holes (FR → task)

`analyze.md` already produced a thorough coverage table. The genuinely missing tasks are:

- **No task** for FR-007 pluggable auth interface definition (analyze.md U1, my R4, my F22)
- **No task** for startup reconciliation (analyze.md I3, my R5)
- **No task** for Windows dev verification in foundational phase (analyze.md G1) — T008a does exist but is in setup phase, not actually validating cross-platform CI gates
- **No task** for HTTP `/api/version` endpoint (analyze.md G2)
- **No task** for `ai` → `ai_reviewer` secret-row population (my F2)
- **No task** for audit-log retention / pruning (my F10)
- **No task** for pending-action expiry worker (my F9)
- **No task** for audit-log tamper-evidence enforcement (my F5)
- **No task** for cross-package import linter (my F7)
- **No task** for release infra to make T136 executable (my F16)

## Alternative approaches still worth weighing

- **Webhook plugins** (per Antigravity's suggestion) — eliminates the sandboxing problem entirely. Plugin authors lose TS-native DX but gain language-agnostic extensibility. Could be offered alongside SDK plugins as "level 2" extension
- **GitOps state model** (my prior F23, still unaddressed in plan) — entire undevops state as declarative files in a git repo, reconciled by the controller. Trades UI-first for file-first; makes backup trivial (`git clone`). Komodo precedent. Not for v0.x but worth a one-paragraph "Why we didn't choose this" in research.md
- **Federation over multi-server cluster** — for orgs with dev/staging/prod in different networks, federation (multiple undevops instances coordinating) may fit better than the current single-controller-with-many-servers model. Not for v0.x, but flag as v0.y direction

## VERDICT

```yaml
verdict: CRITICAL
reviewer: claude
reviewed_at: 2026-05-25T16:15:00Z
commit: 6bf09a25d
critical_count: 3
high_count: 11
medium_count: 11
low_count: 4
```

**Verdict rationale**: Three CRITICAL findings — (F1) the MCP token scope enum is defined three incompatible ways across spec/data-model/quickstart at the most critical security boundary; (F2) `ai → ai_reviewer` migration creates orphan credential refs that will silently break all AI gating; (R1) reinforcing Antigravity F1 — pattern-based secret redaction is broken at both MCP and AI-reviewer boundaries. The HIGH findings concentrate in security (R2, R3, F5, F6), data integrity (F4, F7, F10), and operational completeness (F3, F8, F9). Verdict heuristic says any CRITICAL forces CRITICAL.

**Gate status (constitution Principle VI)**: **FAILING**.
- `analyze.md` verdict: HIGH (not PASS)
- `antigravity.md` verdict: CRITICAL (not PASS)
- `claude.md` verdict (this review): CRITICAL (not PASS)

To unblock `/speckit.implement`, one of:
1. **Address findings, re-review** — recommended path. Fix CRITICALs (F1 enum reconciliation, F2 migration completeness, R1 redaction redesign) + as many HIGHs as practical, then re-run reviewers and aim for ≥2 PASS verdicts
2. **`--override-gate "<reason>"`** — allowed by Principle VI but logged to `_gate-override.md` with timestamp + actor + commit SHA. Frequent overrides on a single feature are themselves an incident

**Next steps recommended**:
1. **Commit plan.md, tasks.md, data-model.md, research.md, quickstart.md, contracts/** if you haven't yet — they're work product. Then snapshot `plan/001-init/v1` and `tasks/001-init/v1` via the standard scripts. Otherwise Principle VII versioning is broken
2. Address F1, F2, R1 (the three CRITICALs) — these are concrete and bounded
3. Address as many HIGHs as fit your appetite; defer LOWs to a polish pass
4. Run `/speckit.review` again from **another tool** (Codex Desktop, Gemini CLI, or Copilot) — Antigravity + this Claude review is two distinct providers but per constitution the gate prefers more diversity. A third independent voice on the post-fix state is cheap insurance
5. Re-run `/speckit.analyze` to verify consistency of the fixes
6. Then `/speckit.implement` will evaluate the gate
