/**
 * @undevops/plugin-sdk — Plugin authoring SDK
 *
 * Provides:
 * - Hook payload types (pre-deploy, post-deploy, deploy-failed, server-*, project-*)
 * - Plugin manifest schema (Zod validation for undevops-plugin.json)
 * - Plugin loader and lifecycle hook dispatcher
 * - Permission declaration and enforcement
 * - Test utilities (createTestContext, mockPayload builders)
 */

export const PLUGIN_SDK_VERSION = "0.1.0";
